# EcoBright Phase 1 portfolio page

Upload these items into your `hib50.github.io` repository preserving the paths:

- `projects/ecobright-phase1.html`
- `assets/images/ecobright-phase1/*`

The HTML already links to `../style.css`, so it will use your existing site styling plus page-specific styles embedded in the page.

The full confidential validation report is intentionally **not** included.
